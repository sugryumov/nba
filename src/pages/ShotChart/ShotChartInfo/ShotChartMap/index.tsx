import { FC } from 'react';
import SVGIcon from '@/components/SVGIcon';

import './index.css';

const ShotChartMap: FC = () => (
  <div className="shot-chart__map">
    <SVGIcon name="floor" width={430} />
  </div>
);

export default ShotChartMap;
